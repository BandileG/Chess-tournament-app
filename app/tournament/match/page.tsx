'use client' import { Suspense, useEffect, useState, useCallback, useRef } from 'react' import { useRouter, useSearchParams } from 'next/navigation' import { createClientComponentClient } from '@supabase/auth-helpers-nextjs' import { Chess } from 'chess.js' import { Chessboard } from 'react-chessboard' type MatchData = { id: string tournament_id: string white_player_id: string black_player_id: string status: string winner_id: string | null current_fen: string white_time_remaining: number black_time_remaining: number time_control: number increment: number move_count: number } type PlayerInfo = { id: string username: string rating: number } // ─── Bot detection ──────────────────────────────────────────────────────────── // Bot UUIDs all start with repeating digits: 11111111-..., 22222222-..., etc. const isBot = (id: string) => /^(\d)\1{7}-/.test(id) function MatchContent() { const router = useRouter() const searchParams = useSearchParams() const matchId = searchParams.get('match_id') const supabase = createClientComponentClient() const [game, setGame] = useState(new Chess()) const [matchData, setMatchData] = useState<MatchData | null>(null) // FIX 1: Store color as 'w' | 'b' internally, only convert to 'white'/'black' for display const [playerColor, setPlayerColor] = useState<'w' | 'b' | null>(null) const [userId, setUserId] = useState<string | null>(null) const [opponent, setOpponent] = useState<PlayerInfo | null>(null) const [myInfo, setMyInfo] = useState<PlayerInfo | null>(null) const [whiteTime, setWhiteTime] = useState(300) const [blackTime, setBlackTime] = useState(300) const [status, setStatus] = useState<'active' | 'completed' | 'loading'>('loading') const [gameResult, setGameResult] = useState<string | null>(null) const [lastMove, setLastMove] = useState<{ from: string; to: string } | null>(null) const [moveHistory, setMoveHistory] = useState<string[]>([]) const [opponentIsBot, setOpponentIsBot] = useState(false) 